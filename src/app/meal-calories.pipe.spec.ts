import { MealCaloriesPipe } from './meal-calories.pipe';

describe('MealCaloriesPipe', () => {
  it('create an instance', () => {
    const pipe = new MealCaloriesPipe();
    expect(pipe).toBeTruthy();
  });
});
